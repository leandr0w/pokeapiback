const { Router } = require('express')

const router = Router()

//router.get('/', findPokemons)

//router.get('/:id', findPokemon)

//router.create('/', createPokemon)

//router.patch('/:id', updatePokemon)

//router.delete('/:id', deletePokemon)

module.exports = {
    pokemonRouter: router,
}